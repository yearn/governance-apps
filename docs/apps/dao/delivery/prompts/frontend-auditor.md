# Frontend and UX Auditor Prompt

```text
Review DAO Governance {WP_ID} read-only in code and, when the route exists, in
the browser at 390px, 768px, 1280px, and a short-height desktop viewport.

Use the shared debug panel or test bridge to reach every package state. Check
proposal scanning, hierarchy, vote totals, no-quorum copy, post-veto
participation, signal wording, script errors, loading/empty/error states,
keyboard flow, focus restoration, touch targets, contrast, wrapping, numeric
stability, and page overflow.

Do not edit. Return blockers before polish suggestions. Cite files and lines for
code findings and attach screenshots for visual findings. State which states and
widths you reviewed.
```
